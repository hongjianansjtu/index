\documentclass{standalone}
\usepackage{tikz}
\begin{document}
\usetikzlibrary{mindmap, trees}
\begin{tikzpicture}
    [mindmap, 
    every node/.style={concept, execute at begin node=\hskip0pt},
    concept color=black!20,
    root concept/.append style={
        concept color=black, fill=white, line width=1ex, text=black, font=\large\scshape},
    grow cyclic,
    text=white,
    level 1/.append style={level distance=4.3cm,sibling angle=90},
    level 2/.append style={level distance=3cm,sibling angle=45}]
    \node [root concept]{Network Security}
    child [concept color=red]{ node {Anonymous Credentials}
        child { node {Multi- Factor} }
        child { node {Lightweight Revocation} }
        child { node {Multiple Authority} }
        child { node {Network Deployment} }
        }
    child [concept color=blue]{ node {Blockchain Security}
        child { node {DPKI in IoT} }
        child { node {Privacy in SSI} }
        child { node {Secure Consensus} }
        child { node {Leader Election} }
        child { node {Routing} }
        child { node {Trust Estimate} }
        }
    child [concept color=orange]{ node {Cloud Security}
        child { node {Access Control} }
        child { node {Trust Outsourcing} }
        child { node {Timed Release} }
        child { node {Multiple Authority} }
        }
    child [concept color=green!40!black]{ node {Formal Proof}
        child { node {Tamarin Prover} }
        child { node {Difficulty Reduction} }
        child { node {Charm Crypto} }
        child { node {Mininet} }
    };
\end{tikzpicture}
\end{document}